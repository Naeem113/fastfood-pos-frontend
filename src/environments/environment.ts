// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  // baseUrl:"https://api.easyinvoices.co/"
  // apiBaseUrl:"https://himsapi.pathcaresoft.com/api/",
  apiBaseUrl:"https://fastfood-pos-alpha.vercel.app/api/",
  // apiBaseUrl:"http://192.168.1.163:8009/api/",
  apiVersion:"v1/",
  // frontend_baseUrl:"http://localhost:4200/#/",
  frontend_baseUrl:"https://dev.easyinvoices.co/#/",

};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/plugins/zone-error';  // Included with Angular CLI.
